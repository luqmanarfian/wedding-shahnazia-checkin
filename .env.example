# Mode Read-Only (Set true saat dideploy ke server publik, false untuk laptop acara lokal)
READ_ONLY_MODE=false

# PIN Akses Panitia
APP_PIN=1234

# Default Google Apps Script Web App URL
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/AKfycbw-sample-wedding/exec

# Auto Pull from Google Sheets on 3-second refresh (true to pull online data automatically, false to only refresh locally)
AUTO_PULL_ENABLED=false

# API base URL for frontend during development
# Set to https://localhost:3000 for HTTPS backend, or http://localhost:3000 for HTTP
VITE_API_URL=https://localhost:3000
